%h2="#{image.name} image"

%p{ :class => 'pull-right'}
  %a{ :class => 'btn', :href => url("/images/#{image.id}/edit") } Edit
  %a{ :class => 'btn', :href => url("/images/#{image.id}/favorite") } Favorite

- show_model(image)
